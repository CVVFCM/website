{# Minimal layout for the embedded (side-pane) Forgie: only the content block is
   rendered — no <html>, header, footer, or importmap. Blocks defined by the child
   template but absent here (title, header, footer, pageclass, importmap…) are ignored. #}
{% block content %}{% endblock %}
